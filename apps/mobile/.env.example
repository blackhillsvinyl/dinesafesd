# No configuration required — the app fetches static data from the public site.
# Optional override for local development (e.g. the web app's dev server,
# which serves the same data files):
# EXPO_PUBLIC_DATA_URL=http://localhost:5173/data
